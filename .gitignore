node_modules/
.env
.env.local
*.log
.DS_Store
pipeline/.cache/
# Cached concall-transcript / PPT text - large, and prompt 7 reads it from the
# same workflow run. Only data/financials.json + data/docs-manifest.json ship.
pipeline/cache/
