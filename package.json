{
  "name": "oilandgas-dashboard",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Oil & Gas Cycle Dashboard - static dashboard plus its own self-contained data pipeline.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fetch:macro": "node pipeline/fetch-macro.mjs",
    "fetch:all": "npm run fetch:macro",
    "test": "node --test \"pipeline/test/*.test.mjs\"",
    "serve": "python3 -m http.server 8000"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "pdf-parse": "^2.4.5",
    "playwright": "^1.49.0"
  }
}
